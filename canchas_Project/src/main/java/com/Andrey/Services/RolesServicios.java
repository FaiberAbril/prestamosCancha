package com.Andrey.Services;
/*
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Repository;
import org.springframework.stereotype.Service;

import com.Andrey.Modelos.Rol;
import com.jair.modelos.Roles;
import com.jair.repositorios.RolesRepository;

@Service
public class RolesServicios {

	@Autowired
	private RolesServicios repository;
	
	public void CrearRol(Rol roles) {
		repository.save(roles);
	}
	
	public void ActualizarRol(Rol roles) {
		repository.save(roles);
	}
	
	public void EliminarRol(long IdRol) {
		repository.deleteById(IdRol);
	}
	
	public Rol BuscarRol(long IdRol) {
		return repository.findById(IdRol).get();
	}
	
	public List<Rol> ListarRoles(){
		return repository.findAll();
	}
	
}
*/